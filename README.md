# 15puzzle

ブラウザで遊べる 15 パズルです。PC とタブレットの両方で操作しやすいように、レスポンシブな 4x4 グリッド UI で実装しています。

## 遊び方

1. `シャッフル` を押してゲーム開始
2. 空きマスに隣接するタイルをタップ / クリックして移動
3. 1〜15 を順番に並べるとクリア

## ローカルで実行

静的ファイルだけで動くため、以下で起動できます。

```bash
python3 -m http.server 8000
```

`http://localhost:8000` を開いてプレイしてください。
